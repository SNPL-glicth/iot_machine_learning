# ═══════════════════════════════════════════
# ZENIN ML — Dependencias de desarrollo
# Generado: 2026-04-07 | Python >=3.10
# ═══════════════════════════════════════════

# Incluir dependencias de producción
-r requirements.txt

# --- Testing ---
pytest>=7.4.0,<9.0.0
pytest-asyncio>=0.21.0,<1.0.0
pytest-cov>=4.1.0,<6.0.0
typeguard>=4.0.0,<5.0.0       # Runtime type checking

# --- Calidad de código ---
ruff>=0.1.0,<1.0.0             # linter + formatter
mypy>=1.7.0,<2.0.0             # type checking

# --- Tipos para mypy ---
types-redis>=4.6.0
types-pyyaml>=6.0.0
